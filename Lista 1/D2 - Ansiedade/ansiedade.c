// 55% Online Judge

/* #include <stdio.h>
#include <string.h>

int main()
{
    char input[4];
    int qtd_triagens = 0, sim = 0, i = 1;

    while ( scanf("%s", input) != EOF )
    {
        if ( i == 10 )
        {
            if ( sim >= 2 )
                qtd_triagens++;

            i = 0;
            sim = 0;
        }

        if ( strcmp(input, "sim") == 0 )
            sim++;

        i++;
    }
    printf("%d\n", qtd_triagens);
    
 return 0;
} */